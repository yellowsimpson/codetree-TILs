#include <stdio.h>

int main() {
    // 여기에 코드를 작성해주세요.
    int a, b;
    char a1, b1;
    scanf("%d %c\n %d %c", &a, &a1, %b, %b1);

    if((a >= 19 && b >= 19) || (a1 == 'm' && b1 == 'm')){
        printf("1");
    }else
        printf("0");
    return 0;
}